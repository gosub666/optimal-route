# 이음국세 — 최단경로 계산기 (로그인 없음)

로그인/팀 관리 기능은 제거하고, **주소 여러 개를 우선순위 기준으로 최단경로 계산**하는
기능만 남긴 버전입니다.

## 1. 패키지 설치
```bash
npm install
```

## 2. 환경변수 (.env.local)
```
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
KAKAO_REST_API_KEY=        # 카카오 로컬 API — 주소→좌표 지오코딩 (서버 전용)
NEXT_PUBLIC_KAKAO_JS_KEY=  # 카카오맵 JS SDK — 지도 렌더링 (브라우저 노출용)
```
(TMAP 앱 키는 더 이상 필요 없습니다 — 경로 순서 계산을 자체 알고리즘으로 대체했습니다.)

## 3. DB 마이그레이션
Supabase SQL Editor에서 `sql/001_reset_route_planner.sql` 실행.
⚠️ 기존 members/teams/route_shares/waypoints 테이블을 모두 삭제하고 새로 만듭니다.

## 4. 기능

- **로그인 없음** — 이 URL에 접근 가능한 누구나 같은 목록을 보고 수정합니다 (내부망/개인용 전제).
- 주소 입력 시 **약속시간(선택)**, **우편물 여부(체크박스)** 함께 입력 가능
- **출발지**: 직접 입력하면 자동으로 기억되어 다음에 기본값으로 채워집니다 (로그인 없이 전역 저장)
- **"최단 경로 계산"** 버튼 → 아래 우선순위로 방문 순서를 계산합니다:
  1. 약속시간이 있는 경유지 — 시간 순서대로 방문
  2. 약속시간이 없는 경유지 — 가까운 약속 위치 근처에 배치 (이동거리 최소화 삽입)
  3. 우편물 — 항상 마지막, 남은 지점 중 가장 가까운 순서로 방문
- 계산된 순서대로 **한 곳씩 티맵/카카오맵 딥링크로 안내** (앱 특성상 다중 목적지 한번에 여는 기능은 지원 안 됨)
- **도착 체크** → "완료 / 부재 / 거부" 방문 결과 선택 팝업 → 저장 후 다음 목적지로 자동 이동

## 5. 알고리즘 참고사항

`lib/routePlanner.ts`의 우선순위 알고리즘은 **휴리스틱(근사)**입니다:
- 거리 계산은 직선거리(Haversine) 기준 — 실제 도로 거리와 차이가 있을 수 있습니다.
- 지점 수가 많아질수록(대략 20~30개 이상) 완전한 최단경로 보장은 어렵습니다.
- 약속시간이 있는 경유지끼리는 절대 순서(시간순)를 어기지 않고, 그 사이/앞뒤에
  일반 경유지를 "삽입 비용이 가장 적은 위치"에 끼워넣는 방식입니다.

## 6. 보안 참고
로그인이 없는 구조라, 이 URL과 Supabase 프로젝트에 대한 접근 자체가 곧 데이터 접근 권한입니다.
필요하면 Vercel의 배포 보호(Password Protection) 기능이나 사내망 제한을 고려하세요.
